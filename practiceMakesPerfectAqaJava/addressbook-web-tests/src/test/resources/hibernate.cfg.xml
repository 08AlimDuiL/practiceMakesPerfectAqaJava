<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>   <!-- Драйвер для MySQL-->
        <property name="connection.url">jdbc:mysql://localhost:3306/addressbook</property> <!-- Адрес базы данных-->
        <property name="connection.username">root</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property> <!-- Язык конкретной СУБД-->

        <!-- Disable the second-level cache -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>   <!-- Показывать SQL-запросы в консоли -->

        <!-- Validate schema, don't create/update -->
        <property name="hbm2ddl.auto">validate</property> <!-- ВАЖНО:Не использовать `create`! -->

        <!-- Names the annotated entity class -->
        <mapping class="ru.stqa.ptf.addressbookMySql.model.GroupData"/>
        <mapping class="ru.stqa.ptf.addressbookMySql.model.ContactData"/><!--  Указываем, какие классы связаны с БД -->

    </session-factory>
</hibernate-configuration>
        <!-- Folder 7.2 -->